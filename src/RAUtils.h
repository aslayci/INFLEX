#ifndef _RA_UTILS_H
#define _RA_UTILS_H

#include <stdlib.h>
#include "arrayUtils.h"
#include "InsertionSort.h"

void createMajorityMatrix(double**, int**, int*, int, int, int);
void createWeightedMajorityMatrix(double**, int**, int*, double*, int, int, int);
void localKemenization(int*, List*, double**, int*, int);





#endif
